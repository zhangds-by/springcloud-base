server:
  port: 8081

spring:
  application:
    name: consul-consumer-client
  cloud:
    # consul注册中心地址
    consul:
      host: 192.168.25.128
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true